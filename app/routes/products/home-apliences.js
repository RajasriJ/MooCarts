import Route from '@ember/routing/route';
import { service } from '@ember/service';

export default class ProductsHomeApliencesRoute extends Route {
  @service router;
  @service('login-credientials') user;

  // setupController()
}
